export function renderPriceTable(data, currentPax, isNonRef) {
    const tbody = document.getElementById('priceTableBody');
    if(!tbody) return;
    
    tbody.innerHTML = ''; // Tøm eksisterende rader
    
    // Her må vi mappe gjennom romtypene dine (Standard, Superior osv)
    // og generere <tr> og <td> basert på prislogikken din.
    // Jeg kan hjelpe deg med den spesifikke loopen hvis du vil ha den nå.
}